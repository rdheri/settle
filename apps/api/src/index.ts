/**
 * SETTLE API entrypoint.
 *
 * Phase 0 placeholder. The Fastify server + outbox poller are wired up in Phase 4.
 */
import { SETTLE_VERSION } from '@settle/shared';

function main(): void {
  console.log(`SETTLE API (v${SETTLE_VERSION}) — server not yet implemented (Phase 4).`);
}

main();
